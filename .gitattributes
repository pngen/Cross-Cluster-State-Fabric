# Cross-Cluster State Fabric - repository text handling.
#
# Sources and metadata are stored with LF and checked out with LF so that the
# same tree builds identically everywhere. MSVC, CMake and Ninja all accept LF.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.lib binary
*.exe binary
